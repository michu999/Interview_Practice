version: '3.8'

services:
  web:
    container_name: django_travel_blog
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
    stdin_open: true
    tty: true

